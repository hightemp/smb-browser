services:
  samba:
    build:
      context: .
    ports:
      - "1445:445"
    healthcheck:
      test: ["CMD-SHELL", "smbclient -L //127.0.0.1/public -U smbtest%synthetic-password -m SMB3 >/dev/null 2>&1 || exit 1"]
      interval: 2s
      timeout: 2s
      retries: 20
    restart: "no"
